import React, { useState } from "react";

// TODO should I convert this store all the state? use use reducer instead? store isLoading?
export default function useAccessToken() {
  return useState("");
}
